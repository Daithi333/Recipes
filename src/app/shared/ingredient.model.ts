export class Ingredient {
    constructor(public name: string, public amount: number) {
    }
}
// In typescript, the setting up of instant variables and calling them in the constructor can be abbreviated to above - adding access modifier directly in the constructor